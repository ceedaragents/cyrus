{
	"name": "cyrus-simple-agent-runner",
	"version": "0.0.2",
	"description": "Simple agent runner for enumerated responses",
	"type": "module",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"files": [
		"dist"
	],
	"scripts": {
		"build": "tsc && npm run postbuild",
		"postbuild": "[ -d dist/simple-agent-runner/src ] && cp -r dist/simple-agent-runner/src/* dist/ && rm -rf dist/simple-agent-runner dist/claude-runner || true",
		"dev": "tsc --watch",
		"test": "vitest",
		"test:run": "vitest run --passWithNoTests",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@anthropic-ai/claude-agent-sdk": "^0.1.11",
		"cyrus-claude-runner": "workspace:*"
	},
	"devDependencies": {
		"@types/node": "^20.0.0",
		"typescript": "^5.3.3",
		"vitest": "^1.1.0"
	},
	"publishConfig": {
		"access": "public"
	}
}
